# 🤖 Marketplace Monitor - Automated Product Tracking System

**Marketplace Monitor** — это автоматизированная система отслеживания новых объявлений на популярных маркетплейсах с мгновенными уведомлениями в Telegram. Настройте фильтры один раз и получайте уведомления о новых товарах автоматически!

## ✨ Основные возможности

### 🔍 Интеллектуальное сканирование
- **Индивидуальные интервалы**: каждый фильтр сканируется с персональной задержкой
- **Гибкая настройка частоты**: точная настройка интервала проверки каждого запроса
- **Умное планирование**: система автоматически определяет готовность фильтров к сканированию
- **Защита от дубликатов**: не показывает уже найденные объявления

### 📱 Telegram интеграция
- **Мгновенные уведомления**: получайте новые товары сразу после их появления
- **Поддержка тредов**: организуйте уведомления по разным каналам и топикам
- **Богатые сообщения**: фотографии, цены, местоположение, прямые ссылки
- **Кнопки действий**: быстрый переход к объявлению

### 🌐 Современный веб-интерфейс
- **Интуитивный дашборд**: удобное управление фильтрами и мониторинг в реальном времени
- **Infinite scroll**: автоматическая подгрузка объявлений при прокрутке
- **Dark mode**: темная тема для комфортной работы
- **Адаптивный дизайн**: оптимизирован для мобильных устройств и десктопа
- **Живая статистика**: API запросы, найденные товары, время работы системы
- **Управление настройками**: изменяйте параметры работы прямо из браузера

### 🚀 Production-ready
- **Railway совместимость**: готовое решение для деплоя в облако
- **PostgreSQL**: надежное хранение данных в production
- **SQLite**: легкий старт для локальной разработки
- **Auto-redeploy**: автоматический перезапуск при критических ошибках
- **Health monitoring**: встроенный мониторинг состояния системы
- **Logging**: подробные логи для отладки и мониторинга

## 🎯 Как это работает

1. **Настройте фильтры**: создайте поисковые запросы на маркетплейсе и добавьте ссылки в систему
2. **Установите интервалы**: укажите, как часто проверять каждый фильтр (рекомендуется 5-10 минут)
3. **Получайте уведомления**: система автоматически находит новые объявления и отправляет в Telegram
4. **Управляйте через веб**: просматривайте найденные товары, изменяйте настройки, анализируйте статистику

## 📋 Быстрый старт

### Вариант 1: Деплой на Railway (рекомендуется для production)

1. **Создайте Telegram бота**:
   ```bash
   1. Откройте @BotFather в Telegram
   2. Создайте бота командой /newbot
   3. Сохраните полученный токен
   4. Узнайте ID чата (напишите @userinfobot)
   ```

2. **Деплой на Railway**:
   ```bash
   # Fork репозитория на GitHub
   # Перейдите на railway.app и создайте новый проект
   # Подключите ваш GitHub репозиторий
   # Railway автоматически определит настройки из Procfile
   
   # Или используйте Railway CLI:
   npm install -g @railway/cli
   railway login
   railway init
   railway add postgresql
   railway up
   ```

3. **Настройте переменные окружения в Railway**:
   ```bash
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   TELEGRAM_CHAT_ID=your_chat_id_here
   PORT=5000
   ```

### Вариант 2: Локальный запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/yourusername/marketplace-monitor.git
   cd marketplace-monitor
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте .env файл**:
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   TELEGRAM_CHAT_ID=your_chat_id_here
   DATABASE_URL=sqlite:///marketplace_monitor.db
   SEARCH_INTERVAL=300
   MAX_ITEMS_PER_SEARCH=50
   ```

4. **Запустите приложение**:
   ```bash
   # Запуск веб-интерфейса и воркера вместе
   python main.py web
   
   # Или отдельно воркер (для сканирования)
   python main.py worker
   ```

5. **Откройте веб-интерфейс**:
   ```
   http://localhost:5000
   ```

## 🔧 Настройка фильтров

1. **Откройте маркетплейс** и настройте нужные фильтры поиска (категория, регион, цена и т.д.)
2. **Скопируйте URL** после применения всех фильтров
3. **Добавьте в систему** через веб-интерфейс (раздел Queries)
4. **Настройте уведомления** для конкретного Telegram чата/треда

### Пример настройки фильтра:
```
Название: "Ноутбуки Apple MacBook Минск"
URL: <ссылка_с_фильтрами_с_маркетплейса>
Telegram Chat ID: -1001234567890
Thread ID: 123 (опционально, для форумов)
Query Refresh Delay: 300 (секунды между проверками)
```

## ⚙️ Основные настройки

### Query Refresh Delay (секунды)
- **Назначение**: интервал между проверками каждого отдельного фильтра
- **Как работает**: каждый фильтр проверяется независимо с указанной задержкой
- **Рекомендуется**: 300-600 секунд (5-10 минут) для оптимальной работы
- **Где настроить**: Configuration → Query Refresh Delay

### Items Per Query
- **Назначение**: максимальное количество объявлений для проверки за один раз
- **Рекомендуется**: 50-100 объявлений
- **Влияние**: больше = меньше пропущенных товаров, но медленнее работа

### Telegram настройки
- **Bot Token**: токен от @BotFather (обязательно)
- **Chat ID**: ID чата для уведомлений (обязательно)
- **Thread ID**: ID треда для форумов (опционально)

## 📊 Мониторинг и статистика

### Веб-дашборд показывает:
- 📈 **Общая статистика**: количество объявлений, API запросов, время работы
- 🔍 **Активные фильтры**: количество настроенных и работающих фильтров
- ⏰ **Последние объявления**: недавно найденные товары с infinite scroll
- 📋 **Системные логи**: подробная информация о работе системы

### Infinite Scroll
- Автоматическая подгрузка новых объявлений при прокрутке страницы
- Работает на страницах Dashboard и Items
- Показывает счетчик "X of Y items"
- Индикатор загрузки при подгрузке новых данных

## 🛠️ Продвинутые настройки

### Прокси поддержка (для обхода блокировок)
```env
PROXY_ENABLED=true
PROXY_LIST=proxy1.example.com:8080,proxy2.example.com:8080
```

### Railway auto-redeploy (автоматический перезапуск)
```env
RAILWAY_TOKEN=your_railway_api_token
RAILWAY_PROJECT_ID=your_project_id
RAILWAY_SERVICE_ID=your_service_id
MAX_ERRORS_BEFORE_REDEPLOY=5
```

### Настройка задержек между запросами
```env
REQUEST_DELAY_MIN=1.0
REQUEST_DELAY_MAX=3.0
SEARCH_INTERVAL=300
```

## 🔍 Устранение проблем

### Система не сканирует объявления
1. **Проверьте логи**: откройте раздел Logs в веб-интерфейсе
2. **Проверьте настройки**: убедитесь, что Query Refresh Delay настроен правильно
3. **Активные фильтры**: проверьте, что фильтры активны (зеленая галочка)

### API запросы не увеличиваются
1. **Проверьте интервал**: фильтры могут быть еще не готовы к сканированию
2. **Логи ошибок**: проверьте наличие ошибок подключения к маркетплейсу
3. **Прокси**: при блокировках включите прокси в настройках

### Telegram уведомления не приходят
1. **Проверьте токен**: убедитесь, что TELEGRAM_BOT_TOKEN правильный
2. **Права бота**: добавьте бота в чат/канал с правами отправки сообщений
3. **Chat ID**: проверьте правильность TELEGRAM_CHAT_ID (для групп начинается с -)

### Веб-интерфейс недоступен
1. **Railway**: проверьте статус deployment в панели Railway
2. **Локально**: убедитесь, что порт 5000 свободен
3. **Переменные**: проверьте настройку PORT и WEB_UI_HOST

## 🎛️ API Endpoints

### Веб-интерфейс
- `GET /` - Главная страница (дашборд)
- `GET /items` - Просмотр всех найденных объявлений
- `GET /queries` - Управление фильтрами поиска
- `GET /config` - Настройки системы
- `GET /logs` - Системные логи

### REST API
- `GET /api/stats` - Статистика в JSON формате
- `GET /api/items?page=1&per_page=30` - Пагинированные объявления
- `POST /api/force-scan` - Принудительное сканирование
- `POST /api/config/save` - Сохранение настроек
- `POST /api/clear-items` - Очистка объявлений

## 📁 Структура проекта

```
marketplace-monitor/
├── main.py                      # Главный файл приложения
├── core.py                      # Логика сканирования и планирования
├── db.py                        # Работа с базой данных (PostgreSQL/SQLite)
├── scraper.py                   # Парсер маркетплейса
├── telegram_worker.py           # Telegram интеграция
├── configuration_values.py      # Настройки и конфигурация
├── web_ui_plugin/              # Веб-интерфейс
│   ├── app.py                  # Flask приложение
│   ├── templates/              # HTML шаблоны
│   │   ├── dashboard.html      # Главная страница
│   │   ├── items.html          # Список объявлений
│   │   ├── queries.html        # Управление фильтрами
│   │   └── config.html         # Настройки
│   └── static/                 # CSS, JS, изображения
│       ├── css/style.css       # Стили (с dark mode)
│       └── js/app.js           # JavaScript логика
├── marketplace_api/             # Модуль работы с API
├── Procfile                     # Конфигурация для Railway
├── requirements.txt             # Python зависимости
└── runtime.txt                  # Версия Python
```

## 🔄 Архитектура планировщика

1. **Каждую минуту** планировщик проверяет готовность всех фильтров
2. **Для каждого фильтра** сравнивается время последнего сканирования с Query Refresh Delay
3. **Готовые фильтры** сканируются независимо друг от друга
4. **После сканирования** обновляется метка времени последней проверки
5. **Найденные товары** отправляются в Telegram и сохраняются в базе данных

## 🎨 Особенности интерфейса

### Dark Mode
- Автоматическое сохранение выбранной темы
- Плавная анимация переключения
- Оптимизированные цвета для обеих тем

### Адаптивный дизайн
- Desktop: 4 колонки карточек
- Tablet: 3 колонки карточек
- Mobile: 2 колонки карточек
- Оптимизированная форма поиска для мобильных устройств

### Infinite Scroll
- Автоматическая подгрузка при достижении конца страницы
- Индикатор загрузки
- Динамический счетчик элементов
- Работает с фильтрами и поиском

## 🤝 Вклад в проект

Мы приветствуем любой вклад в развитие проекта:

1. **Issues**: сообщайте об ошибках с подробным описанием
2. **Features**: предлагайте новые функции и улучшения
3. **Pull Requests**: отправляйте PR с исправлениями и новыми возможностями
4. **Documentation**: помогайте улучшать документацию

## 📝 Changelog

### v1.3.0 (текущая версия)
- ✅ **Добавлено**: Infinite scroll для Dashboard и Items
- ✅ **Улучшено**: Адаптивный дизайн для мобильных устройств
- ✅ **Обновлено**: Счетчики статистики в стиле современных приложений
- ✅ **Исправлено**: Форма поиска теперь корректно отображается на мобильных

### v1.2.0
- ✅ **Исправлено**: логика планировщика с Query Refresh Delay
- ✅ **Добавлено**: индивидуальное отслеживание времени сканирования
- ✅ **Исправлено**: счетчик API запросов
- ✅ **Улучшено**: кликабельные теги объявлений

### v1.1.0
- ✅ Веб-интерфейс с управлением фильтрами
- ✅ PostgreSQL поддержка для Railway
- ✅ Auto-redeploy система
- ✅ Улучшенное логирование

### v1.0.0
- ✅ Базовое сканирование маркетплейса
- ✅ Telegram уведомления
- ✅ SQLite поддержка

## 📄 Лицензия

MIT License - свободное использование в личных и коммерческих целях.

## 🔐 Конфиденциальность

Система работает полностью автономно на вашем сервере. Все данные хранятся в вашей базе данных. Мы не собираем и не передаем никакую информацию третьим лицам.

---

**Marketplace Monitor** - автоматизируйте поиск нужных товаров! 🚀
