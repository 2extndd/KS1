# 🔧 Troubleshooting Guide - KufarSearcher Bot

## 📋 Общие проблемы и решения

### 1. ❓ "Бот не скидывает все вещи из ссылки"

**Это НЕ баг! Это правильное поведение.**

**Как работает:**
- `Items Per Query` - это **ГЛОБАЛЬНЫЙ параметр** для ВСЕХ queries
- Применяется ко всем поисковым запросам одновременно
- Определяет сколько **НОВЫХ** объявлений брать при каждом сканировании
- Kufar.by показывает объявления **от НОВЫХ к СТАРЫМ**
- Бот берёт только **первые N объявлений** (самые новые)
- Старые объявления уже были обработаны в предыдущих сканированиях

**Пример с Items Per Query = 2 (глобально для всех queries):**
```
Query 1 (Электроника):
  Страница Kufar (50 объявлений):
  1. iPhone 15 (5 мин назад)   ← БОТ БЕРЁТ
  2. Samsung S23 (10 мин назад) ← БОТ БЕРЁТ
  3. Xiaomi 13 (15 мин назад)   ← Пропускает
  ...

Query 2 (Недвижимость):
  Страница Kufar (30 объявлений):
  1. Квартира 3к (8 мин назад)  ← БОТ БЕРЁТ
  2. Дом в деревне (12 мин)     ← БОТ БЕРЁТ
  3. Участок 10 соток (20 мин)  ← Пропускает
  ...
```

**Решение:**
- Увеличьте `Items Per Query` в `/config` до 10-50
- Это применится ко **ВСЕМ** queries одновременно
- Для популярных категорий: `Items Per Query = 20-50`
- Для редких категорий: `Items Per Query = 5-10`
- Если у вас mix популярных и редких queries, выбирайте среднее значение (10-20)

---

### 2. ⏱️ "Query Refresh Delay не соблюдается"

**Это работает правильно, просто неоптимально.**

**Как работает:**
- `Query Refresh Delay` - это **ГЛОБАЛЬНЫЙ параметр** для ВСЕХ queries
- Применяется ко всем поисковым запросам одновременно
- Планировщик проверяет queries каждую минуту
- Но сканирует только те, у которых прошло >= `Query Refresh Delay` секунд
- Если интервал = 300 сек (5 мин), то 4 из 5 минут будут пустые проверки

**Это видно в логах:**
```
[2024-01-01 10:00:00] Checking queries... skipped_searches=3
[2024-01-01 10:01:00] Checking queries... skipped_searches=3
[2024-01-01 10:02:00] Checking queries... skipped_searches=3
[2024-01-01 10:03:00] Checking queries... skipped_searches=3
[2024-01-01 10:04:00] Checking queries... skipped_searches=3
[2024-01-01 10:05:00] Scanning 3 queries... (finally!)
```

**Важно:** Все queries сканируются с **ОДИНАКОВЫМ** интервалом!
- Query 1: сканируется каждые 300 секунд
- Query 2: сканируется каждые 300 секунд  
- Query 3: сканируется каждые 300 секунд

**Решение:**
- Это не критично, просто лишние записи в логах
- Queries всё равно сканируются с правильной частотой
- Можно оптимизировать планировщик (будущее улучшение)

---

### 3. 🔘 "Force Scan кнопка не работала"

**Статус:** ✅ ИСПРАВЛЕНО

**Что было:**
- Кнопка отсутствовала в UI
- API endpoint существовал, но не было способа его вызвать

**Что сделано:**
- Добавлена кнопка "Force Scan All Queries" на Dashboard
- Добавлена кнопка "Force Scan All Queries Now" на Queries странице
- JavaScript функция с loading индикатором

**Как использовать:**
1. Откройте Dashboard или Queries
2. Нажмите кнопку "Force Scan"
3. Дождитесь результата
4. Проверьте новые items в `/items`

---

### 4. ⚙️ "Config страница не сохраняет настройки"

**Статус:** ✅ ИСПРАВЛЕНО

**Что было:**
- Форма не загружала текущие значения из БД
- Поля были пустые при открытии `/config`
- Создавалось впечатление, что сохранение не работает

**Что сделано:**
- Backend теперь передаёт текущие значения (bot_token, chat_id, proxy_list)
- Frontend отображает эти значения в форме
- Можно видеть что уже настроено

**Как использовать:**
1. Откройте `/config`
2. Все поля должны быть заполнены текущими значениями
3. Измените нужные параметры
4. Нажмите "Save Configuration"
5. Проверьте что изменения применились

---

### 5. 📱 "Некоторые items не присылаются в Telegram"

**Возможные причины:**

#### A) Telegram Bot Token или Chat ID неправильные
**Проверка:**
1. Откройте `/config`
2. Убедитесь что Bot Token и Chat ID заполнены
3. Проверьте `/logs` на ошибки `Failed to send telegram notification`

**Решение:**
1. Получите новый Bot Token от @BotFather
2. Узнайте свой Chat ID (@userinfobot)
3. Сохраните в `/config`

#### B) Item уже был отправлен ранее (дубликат)
**Проверка:**
1. Откройте `/logs`
2. Ищите `[DUPLICATE]` для этого item

**Решение:**
- Это нормальное поведение! Дубликаты не отправляются повторно

#### C) Ошибка при отправке Telegram сообщения
**Проверка:**
1. Откройте `/logs`
2. Ищите:
   - ✅ `Telegram notification sent successfully` - успех
   - ⚠️ `Telegram notification failed` - неудача
   - ❌ `Failed to send telegram notification` - ошибка

**Решение:**
- Проверьте интернет-соединение
- Проверьте что бот не заблокирован в Telegram
- Проверьте что Chat ID правильный

#### D) Item добавлен в БД, но не помечен как "sent"
**Проверка:**
1. Откройте `/items`
2. Найдите item
3. Проверьте колонку "Sent" (должно быть ✅)

**Решение:**
- Используйте кнопку "Resend All Unsent" на `/items`
- Или перезапустите worker процесс

---

### 6. 🔄 "В логах много [DUPLICATE]"

**Это нормально! Не баг.**

**Почему:**
- Kufar показывает одни и те же объявления при каждом сканировании
- Бот проверяет каждый item по `kufar_id`
- Если item уже есть в БД, он не добавляется повторно
- Логируется как `[DUPLICATE]` для отслеживания

**Пример:**
```
Скан 1: [Item A, Item B] → добавлено 2
Скан 2: [Item C, Item A] → добавлено 1, дубликат 1
Скан 3: [Item D, Item C] → добавлено 1, дубликат 1
```

**Решение:**
- Не требуется! Это правильное поведение
- [DUPLICATE] означает что система работает корректно

---

## 📊 Диагностика проблем

### Шаг 1: Проверьте Dashboard
1. Откройте `/`
2. Проверьте:
   - Queries: должны быть active queries
   - Items: должны появляться новые items
   - API Requests: должно расти при сканировании
   - Uptime: должно расти (значит worker работает)

### Шаг 2: Проверьте Logs
1. Откройте `/logs`
2. Ищите последние записи:
   - `Scanning N queries` - сканирование запущено
   - `[NEW]` - новый item добавлен
   - `[DUPLICATE]` - дубликат (норма)
   - `ERROR` - ошибка (нужно исправить)

### Шаг 3: Проверьте Config
1. Откройте `/config`
2. Проверьте все поля заполнены:
   - Bot Token ≠ пустое
   - Chat ID ≠ пустое
   - Items Per Query = 10-50 (рекомендуется)
   - Query Refresh Delay = 300 сек (по умолчанию)

### Шаг 4: Используйте Force Scan
1. Откройте Dashboard или Queries
2. Нажмите "Force Scan All Queries"
3. Проверьте результат в уведомлении
4. Проверьте новые items в `/items`

---

## 💡 Оптимальные настройки

⚠️ **ВАЖНО:** `Items Per Query` и `Query Refresh Delay` - это **ГЛОБАЛЬНЫЕ** параметры!
Они применяются ко **ВСЕМ** queries одновременно.

### Если ВСЕ ваши queries - популярные категории (электроника, авто):
```
Items Per Query: 30-50
Query Refresh Delay: 300 секунд (5 минут)
```

### Если ВСЕ ваши queries - средняя активность (одежда, аксессуары):
```
Items Per Query: 10-20
Query Refresh Delay: 300 секунд (5 минут)
```

### Если ВСЕ ваши queries - редкие категории (специфичные товары):
```
Items Per Query: 5-10
Query Refresh Delay: 600 секунд (10 минут)
```

### Если у вас MIX популярных и редких queries:
```
Items Per Query: 15-20  (среднее значение)
Query Refresh Delay: 300 секунд (5 минут)
```
**Объяснение:** Популярные queries получат достаточно items, редкие - не будут перегружены.

### Для срочного мониторинга ВСЕХ queries:
```
Items Per Query: 20-30
Query Refresh Delay: 60-120 секунд (1-2 минуты)
```
⚠️ **Внимание:** Частые запросы могут привести к блокировке IP на Kufar.by

---

### 💡 Совет: Группируйте queries по активности
Если у вас есть и популярные, и редкие категории:
1. Создайте отдельные инстансы бота для разных типов queries
2. Или выберите среднее значение параметров (Items Per Query = 15-20)

---

## 🆘 Всё ещё не работает?

1. **Перезапустите приложение:**
   - Railway: используйте кнопку Redeploy
   - Локально: `Ctrl+C` и запустите снова

2. **Проверьте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` - должен быть заполнен
   - `TELEGRAM_CHAT_ID` - должен быть заполнен
   - `DATABASE_URL` - должен указывать на PostgreSQL (Railway) или SQLite (локально)

3. **Проверьте логи Railway:**
   - Откройте Railway Dashboard
   - Перейдите в раздел Logs
   - Ищите ошибки при запуске

4. **Проверьте базу данных:**
   - Откройте `/items` - должна быть таблица с items
   - Откройте `/queries` - должны быть добавленные queries
   - Если таблицы пустые, БД не инициализирована

---

## 📚 Дополнительная информация

- **README.md** - Основная документация
- **TROUBLESHOOTING.md** - Этот файл
- **Logs** (`/logs`) - Реальное время логи
- **Dashboard** (`/`) - Статистика и состояние
