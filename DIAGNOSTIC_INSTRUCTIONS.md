# 🔍 ИНСТРУКЦИЯ ПО ДИАГНОСТИКЕ ПРОБЛЕМЫ

## Проблема: Настройки не сохраняются на Railway

---

## ШАГ 1: Проверьте текущие значения

1. Откройте `/config` на Railway
2. Посмотрите что показано в полях:
   - **Items Per Query**: ?
   - **Query Refresh Delay**: ?

---

## ШАГ 2: Попробуйте сохранить

1. Измените **Items Per Query** на **77**
2. Нажмите "Save Configuration"
3. Подождите 3 секунды
4. Страница перезагрузится

---

## ШАГ 3: ПРОВЕРЬТЕ ЛОГИ (КРИТИЧЕСКИ ВАЖНО!)

1. Откройте `/logs` на Railway
2. Найдите записи **ПОСЛЕ** нажатия "Save Configuration"
3. Скопируйте и пришлите мне строки:

```
Должно быть примерно так:

📥 Received config save request: {"max_items_per_search": 77, ...}
📝 Settings to save: {'MAX_ITEMS_PER_SEARCH': '77', ...}
✅ Configuration saved: MAX_ITEMS_PER_SEARCH = 77
✅ Setting saved and verified: MAX_ITEMS_PER_SEARCH = 77
🔍 Verifying saved settings...
   MAX_ITEMS_PER_SEARCH: saved='77', read='77'
✅ Configuration save complete: 2 saved, 0 failed
```

**ИЛИ может быть ошибка:**

```
❌ Setting saved but verification FAILED: MAX_ITEMS_PER_SEARCH (expected=77, got=3)
```

---

## ШАГ 4: Проверьте что вернулось

1. Обновите `/config` (F5)
2. **Items Per Query** показывает **77** или **3**?

---

## ШАГ 5: Проверьте откуда берётся значение

1. Откройте `/logs`
2. Нажмите кнопку "Scan" (запустите сканирование)
3. Найдите строку:

```
🔧 MAX_ITEMS_PER_SEARCH from database (WebUI): 77 -> 77 (env=3 IGNORED)
```

**ИЛИ:**

```
🔧 MAX_ITEMS_PER_SEARCH from environment (no DB setting): 3 -> 3
```

Это покажет **ОТКУДА** берётся значение!

---

## ШАГ 6: Проверьте Railway Variables

1. Откройте Railway Dashboard
2. Перейдите в ваш проект
3. Нажмите "Variables"
4. Найдите переменные:
   - `MAX_ITEMS_PER_SEARCH`
   - `SEARCH_INTERVAL`

**Если они установлены:**
- Запишите их значения
- Попробуйте их **УДАЛИТЬ**
- Сделайте **Redeploy**

---

## ШАГ 7: Пришлите мне информацию

Скопируйте и пришлите:

```
1. Логи из /logs после сохранения config:
   [вставьте сюда]

2. Логи из /logs после нажатия Scan:
   [вставьте сюда]

3. Railway Variables (если есть):
   MAX_ITEMS_PER_SEARCH = ?
   SEARCH_INTERVAL = ?

4. Что показывает /config после перезагрузки:
   Items Per Query = ?
```

---

## ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ:

### Проблема A: Railway env переопределяет БД
**Симптомы:**
```
🔧 MAX_ITEMS_PER_SEARCH from environment (no DB setting): 3 -> 3
```
**Решение:** Удалить env переменные из Railway Variables

---

### Проблема B: БД не сохраняет
**Симптомы:**
```
❌ Setting saved but verification FAILED: MAX_ITEMS_PER_SEARCH (expected=77, got=NULL)
```
**Решение:** Проблема с PostgreSQL - нужно проверить права доступа

---

### Проблема C: БД сохраняет, но не читает
**Симптомы:**
```
✅ Setting saved and verified: MAX_ITEMS_PER_SEARCH = 77
🔧 MAX_ITEMS_PER_SEARCH from environment: 3 -> 3
```
**Решение:** Кеширование или env переопределяет при чтении

---

### Проблема D: PostgreSQL commit не завершается
**Симптомы:**
```
✅ Configuration saved: MAX_ITEMS_PER_SEARCH = 77
(но при проверке вернулось старое значение)
```
**Решение:** Увеличить задержку или проверить транзакции

---

## БЕЗ ЛОГОВ Я НЕ МОГУ ПОМОЧЬ!

Пожалуйста, пришлите логи из `/logs` - там будет видно ВСЁ!
